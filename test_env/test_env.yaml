name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pymatgen>=2022.3.22
  - MDAnalysis
  - openmm
  - numpy
  - pytest
  - openbabel
  - rdkit
  - openff-toolkit>=0.12.0
  - packmol
  - pydantic

    # Pip-only installs
  - pip:
      - pymatgen
